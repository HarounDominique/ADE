node_modules/
dist/
.DS_Store
.ade/*.db
# Built installers and their manifest: artifacts, not sources. Anchored,
# because a bare pattern would also swallow src/application/release.
/release/

# Hot cache — never a truth source, never committed. See memory-bank-schema.md.
memory-bank/.local/

# Python
__pycache__/
*.pyc
.venv/
*.egg-info/
.pytest_cache/

# OS / editor noise
Thumbs.db
