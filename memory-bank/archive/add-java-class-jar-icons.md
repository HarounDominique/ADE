# Archive: add-java-class-jar-icons

**Spec**: `memory-bank/specs/SPEC-add-java-class-jar-icons.md`
**Task**: `memory-bank/tasks/add-java-class-jar-icons.md` (fast-path, inline reflection)
**Branch**: `task/add-java-class-jar-icons`

## What was built

Added `.class` (`javaclass.svg`) and `.jar` (`jar.svg`) to the Explorer's file-icon
mapping — both icons already existed in the vendored upstream set but were missed during
the original curation pass. Operator noticed the gap directly.

## Satisfies

`SPEC-add-java-class-jar-icons.md` — Objective and Boundaries, in full.

## Deviations accepted

None — matched the spec exactly.
