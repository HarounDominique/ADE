{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "editor-window",
  "description": "Capability for a document opened in a window of its own. It edits one file and closes itself; it has no reason to open further windows.",
  "windows": [
    "editor-*"
  ],
  "permissions": [
    "core:default",
    "core:window:allow-destroy",
    "core:window:allow-set-title"
  ]
}
